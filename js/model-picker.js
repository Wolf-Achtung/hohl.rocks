/* model-picker.js */(function(){async function fetchModels(){try{const r=await fetch('/api/models.json',{cache:'no-store'});return await r.json();}catch(e){return[]}}async function openModelPicker(){const arr=await fetchModels();if(!arr.length){openAnswerPopup('Model‑Picker: keine Daten in /api/models.json gefunden.');return;}const lines=arr.map(m=>`• ${m.name} — ${m.tier}\n  Stärken: ${m.strengths.join(', ')}\n  Bestens für: ${m.best_for.join(', ')}\n  Beispiel: ${m.sample_prompt}`).join('\n\n');openAnswerPopup('Model‑Picker\n\n'+lines);}document.addEventListener('DOMContentLoaded',()=>{const n=document.querySelector('#nav-buttons .news');if(n){n.addEventListener('dblclick',openModelPicker);}window.openModelPicker=openModelPicker;});})();